import { SeriesConfig } from "../types";

export const PIE_CONFIG: SeriesConfig = {
    name: "three-auto-map",
    id: 0,
    type: "map",
    castShadow: false,
    receiveShadow: false,
    itemStyle: {},
}